# atoztem 자동화 파이프라인 — 프로젝트 현황

마지막 갱신: 2026-08-06 23:42 (KST) — git status/diff로 검증 완료(clean, HEAD=origin/main=8f87f1c)
저장소: `github.com/dannydanny11/sns-` (public 필수 — private 전환 금지)
계정: 인스타그램 `atoztem` (쿠팡파트너스 제휴, 기존 팔로워 558+)
링크 허브: https://dannydanny11.github.io/sns-/
스토리 페이지: https://dannydanny11.github.io/sns-/story.html

---

## 1. 무엇을 하는 프로젝트인가

인스타 계정 `atoztem`에 쿠팡파트너스 제휴 상품을 **완전/반자동으로** 소개하는 콘텐츠를 매주 생성·게시한다. GitHub Actions 크론이 클라우드에서 돌아가므로 사용자 PC가 꺼져 있어도 동작한다.

**핵심 루프**: 쿠팡 API로 상품 선정 → 제휴 딥링크 생성 → Claude API로 훅/카피/내레이션 생성 → 카드·릴스 렌더 → 인스타 Graph API로 게시 → 프로필 링크 페이지 갱신.

---

## 2. 확정된 전략 (왜 이렇게 짰는가)

### 2-1. 주간 풀 구조 (2026-08-04 대개편)
과거엔 "매일 새 상품 5개 뽑아 캐러셀+릴스 동시 게시"였다. 이를 폐기하고 **주간 풀** 구조로 전환:

- **월요일**: 상품 10개 풀을 한 번에 확정 (4개 카테고리 라운드로빈으로 다양성 확보) + 딥링크·훅·카피·내레이션을 이 시점에 일괄 생성해 저장.
- **평일(화~금) 08시·19시**: 풀에서 상품 1개씩 꺼내 **단품 릴스**로 게시 (하루 2개, 10슬롯 = 10상품).
- **금요일 20시**: 그 주 10개 중 5개를 골라 **캐러셀**로 재구성해 게시.
- **매일**: 스토리 카드 자동 생성 (반자동, 아래 2-4 참고).

**이유**: 상품 풀 하나에서 릴스(단품)·캐러셀(묶음)·스토리가 전부 파생되므로 소재 수집을 반복할 필요가 없고, 같은 상품이 릴스→캐러셀로 두 번 노출되는 것도 반복 노출 효과로 전환에 유리하다는 판단(사용자 결정).

### 2-2. 크론 지연 대응 — "슬롯 기준" 게시
GitHub Actions의 `schedule`은 정시 보장이 안 되고(best-effort), 실측 1~2시간 지연이 흔하다. 이를 회피하기 위해 **예정 시각을 절대 기준으로 삼지 않는다**:

- 각 릴스 슬롯은 "예정 시각"을 가지며, 매 실행 시 "예정 시각이 지났는데 아직 안 올린 슬롯"만 게시한다(`dueReelSlots`, 최대 2개까지 한 번에 따라잡기).
- 즉 크론이 늦게 떠도 언젠가는 반드시 밀린 슬롯을 처리한다 — **놓치는 게시가 없다.**
- 주중에 파이프라인을 처음 가동할 때는 이미 지나간 요일의 슬롯을 `skipPastSlots`로 건너뛰어(posted+skipped 마킹) "오늘부터 깔끔하게" 시작하게 했다.
- `workflow_dispatch`에 **`force` 입력**을 추가해 예정 시각과 무관하게 "다음 미게시 슬롯 1개"를 즉시 올릴 수 있게 함 — 크론이 늦을 때 사용자가 직접 트리거하는 안전판.

**실측 결론(중요)**: 크론은 "고장"이 아니라 매번 늦게라도 뜬다. 화요일 아침 08:00 예정 → 실제 09:03 도착, 저녁 19:00 예정 → 실제 21:00 도착. 사용자가 크론보다 먼저 force 버튼을 누르면 크론이 도착했을 때 "할 일 없음"으로 지나가므로 "자동이 안 됐다"고 오해하기 쉽다. 실제로는 **아침도 저녁도 매번 자동으로 갔다** (그냥 기다리면 온다). 유료 GitHub 플랜으로 바꿔도 crontab 정시 보장은 되지 않는다는 점을 사용자에게 설명함. 정시 트리거가 필요하면 외부 스케줄러(cron-job.org 등)로 `workflow_dispatch`를 API 호출하는 방법을 제시했으나 **아직 구현 안 함** (사용자가 일단 지연 감수하며 지켜보기로).

**추가 실측(2026-08-04~06, 3일치 워크플로 이력 확인)**: 이 기간 실행 8건 중 **schedule(자동) 6건 / workflow_dispatch(수동 force) 2건** — 슬롯4(수 오전)만 사용자가 직접 force로 올렸고, 슬롯5·6·7(수 저녁~목 저녁)은 전부 **크론이 스스로 처리**했다(전부 success). 즉 초반에 "아침마다 안 된다"고 느꼈던 건 매번 먼저 force를 눌러서였고, 그냥 두면 실제로 자동 게시율이 매우 높다는 게 반복 확인됨.

### 2-3. 릴스 완주율 최적화 (인사이트 기반, 2026-08-04)
사용자가 실제 인스타 릴스 인사이트를 공유(평균 조회 2초, 건너뛰기 97.5%)한 뒤 개선. 이후 사용자가 공유한 "릴스 그래프 4유형" 외부 가이드(TYPE01~04, 훅 약함/CTA 실패/알맹이부족/중간이탈)를 프롬프트·타이밍에 반영:

- **훅**: 결과·대상형(예: "책상이 달라짐"), 인사·빌드업 금지, 4~9자.
- **첫 내레이션 문장 = 두 번째 훅**: 무음 시청자 대응, 제품의 가장 강한 셀링포인트를 결론부터 즉시 말함(인트로 없음).
- **본론**: 반복 금지, 한 문장 새 정보 하나, 일상어. CTA·인사 문구는 본론에 넣지 않음(순수 알맹이).
- **CTA는 끝 한 곳만**: 릴스 마지막 화면에서 "이번 주 다른 추천템" 미니모음 + 프로필 링크 유도, 단일 행동만 요청.
- **타이밍**: TTS 속도 +16%→**+30%**, 대사 뒤 여유 0.45→0.18초, 리드 타임 0.12→0.06초(텀 단축). 부족한 총 길이는 본문이 아니라 **마지막 CTA 카드에만** 몰아서 20초 하한을 채움(본문 텀은 그대로 짧게 유지).
- **화면 리듬**: 장점 슬라이드마다 같은 제품 이미지를 다른 확대/구도(전체→위쪽 클로즈업→중앙 디테일→아래쪽 등 6종 순환)로 보여줌 + 카드마다 미세한 켄번스 줌 — "이미지가 안 바뀐다"는 사용자 피드백 대응. (쿠팡 파트너스 API는 상품 이미지를 1장만 제공하므로 "여러 실제 컷"은 불가능 — 구도 변화로 대체)

**실측 효과(1편 비교, 표본 작음— 5~10편 누적 후 재판단 필요)**: 평균 조회 2초→4초, 건너뛰기 97.5%→77.9%(평균 이하로 반전), 도달 82→105, 조회 84→112. 좋아요/저장/공유는 여전히 0 — 참여 유도는 미해결 과제.

### 2-4. 스토리 = 반자동 (링크 스티커는 API 자동 불가)
"1릴스 1캐러셀 1스토리" 권고를 논의했으나 그대로 따르지 않고 우리 상황에 맞춤:
- 릴스 2/일(완전자동), 캐러셀 1/주(완전자동)는 유지.
- 스토리는 **Meta Graph API가 링크 스티커를 서드파티에 개방하지 않아서**(`media_type=STORIES`로 이미지/영상 게시는 가능하나 스티커·링크·설문 등 인터랙티브 요소는 API로 절대 못 붙임 — 공식 제약, 웹서치로 확인함) 완전자동화가 불가능하다.
- 대신 **반자동**: 파이프라인이 릴스 슬롯마다 스토리 카드(9:16, 훅+제품+가격+"위 링크 눌러 구매" 안내)를 자동 렌더해 `docs/stories/`에 저장하고, `docs/story.html`("오늘의 스토리" 페이지)에 이미지+딥링크 복사 버튼을 올려둔다.
- 사용자는 매일 아침 폰에서 그 페이지를 열어 ①이미지 저장 ②스토리 업로드 ③링크 스티커에 [링크 복사] 버튼으로 붙여넣기 — 하루 10초.
- 릴스·캐러셀·피드에는 걸 수 없는 **직접 클릭형 구매 링크**를 스토리가 유일하게 제공하므로, 자동화가 못 채우는 이 10초가 오히려 전환율이 가장 높은 지점이라는 게 이 설계의 논리.

### 2-5. 링크 정확성 (과거 반복 이슈, 이제 해결됨)
쿠팡 상품은 옵션(itemId/vendorItemId)마다 이미지가 다르다. 초기엔 productId만으로 딥링크를 만들어 "카드 이미지와 실제 링크 상품이 다르다"는 사용자 피드백을 반복해서 받았다. 현재는 `productUrl`에서 `itemId`·`vendorItemId`를 파싱해 `coupang.com/vp/products/{pageKey}?itemId=..&vendorItemId=..` 형태로 딥링크를 만들어 카드 이미지=링크 상품 일치를 보장한다(`pipeline.js`의 `rawProductUrl`).

---

## 3. 게시물 3대 필수조건 (절대 깨면 안 됨)

1. **저장소는 반드시 public.** 다른 세션이 "신원 분리" 목적으로 private 전환 + 이름변경(sns-automation)을 시도해 링크 페이지가 이틀간 404 나는 사고가 있었음(2026-08-03~04). 복구했고, 사용자에게 "이 저장소는 public·현재 이름(`sns-`) 유지 필수, 신원 분리는 이메일 프라이버시로 해결"이라고 명확히 전달함. **저장소 이름도 다시 바꾸지 말 것** — GitHub Pages 주소가 대소문자를 구분해 혼란을 일으킨 적 있음(`SNS-` vs `sns-`).
2. 카드 이미지는 **JPEG**만 (PNG은 인스타 Graph API가 거부함).
3. 캐러셀 컨테이너는 **status_code=FINISHED 확인 후** 발행해야 함("Media ID is not available" 에러 방지) — `instagram.js`의 `waitReady`로 구현됨.

---

## 4. 파일 구조 (핵심 모듈)

```
src/
  weekPool.js       주간 풀 상태·슬롯 스케줄링 코어 (신규, 2026-08-04)
                     weekKeyOf/slotInstant/carouselInstant/buildSchedule/
                     dueReelSlots/carouselDue/pickCarousel/skipPastSlots/
                     nextUnpostedReel/markReelPosted/markCarouselPosted
  pipeline.js       generateDue() / publishDue() — 주간 풀 오케스트레이션
                     (구 generate()/publish()는 폐기됨)
  selectProducts.js selectWeeklyPool() — 카테고리 라운드로빈 10개 선정 (신규 함수 추가)
                     pickCategory/selectProducts는 구버전 잔재, 현재 미사용
  caption.js        generatePoolContent() — 풀 10개 훅/카피/내레이션/태그 일괄생성
                     buildReelCaption() — 단품 릴스 캡션 조립
                     generateCaption() — 캐러셀(금요일 묶음) 캡션, 구버전에서 유지
  cards/
    build.js          buildSingleReelCards() — 단품 릴스 카드셋(표지+장점N+CTA)
                       buildStoryImage() — 스토리 카드 1장
                       buildPostCards() — 캐러셀 카드셋(구버전 유지, 금요일에 사용)
    reelTemplates.js   buildReelSingleCover/buildReelBenefit/buildReelTeaserCta (신규)
                        buildReelCover/Product/Cta는 구버전 잔재
    storyTemplate.js   buildStoryCard() (신규)
    theme.js           확정 시안 C(소프트 크림)
  reel.js           카드 이미지 → mp4 (ffmpeg-static). TTS 내레이션 + 배경음 + 켄번스 줌
  storyPage.js      docs/story.html 생성 (신규)
  linkPage.js       docs/index.html 생성 (프로필 링크 허브, 이번주+지난주 아카이브)
  archive.js        link-archive.json 누적 관리
  instagram.js      Graph API 캐러셀/릴스 게시, waitReady 폴링
  coupang/          client.js(HMAC서명) / search.js / deeplink.js
  postedLog.js       postedTodayKST 등 (구버전 가드, 현재 주간풀 가드로 대체됨—미사용 가능성 확인 필요)

scripts/
  generate.js       generateDue() 호출, 결과 텔레그램 통지
  publish.js        publishDue() 호출, 결과 텔레그램 통지

.github/workflows/pipeline.yml
  cron: 평일 08/19시(KST) 릴스, 금 20시 캐러셀, 평일 12시 안전망 따라잡기
  workflow_dispatch inputs: dry_run, force(즉시 다음 릴스 1개)

data/
  week-pool.json    이번 주 풀 + 슬롯 게시 상태 (자동 커밋됨)
  stories.json       스토리 카드 이력
  link-archive.json  링크 페이지 아카이브
  posted_log.json    구버전 게시 로그(레거시)

docs/
  index.html        프로필 링크 허브 (GitHub Pages)
  story.html         오늘의 스토리 페이지 (신규)
  stories/           스토리 카드 이미지들
  .nojekyll
```

`src/shorts/*`는 별개 프로젝트(유튜브 쇼츠 자동화)로 이 파이프라인과 무관 — 같은 리포에 공존.

---

## 5. 현재 라이브 상태 (2026-08-06 23:42 KST 재확인 — git fetch로 origin과 일치 검증)

이번 주(weekKey 2026-08-03, 월요일 시작) 진행 상황. 목요일 밤 기준 화~목 전 슬롯이 완료된 상태:

| 슬롯 | 라벨 | 상태 |
|---|---|---|
| 0 | 월 오전 | skipped (주중 가동 시작이라 건너뜀) |
| 1 | 월 저녁 | skipped |
| 2 | 화 오전 | ✅ 게시됨 (reelId 18604374553008627) |
| 3 | 화 저녁 | ✅ 게시됨 — 자동(schedule), 크론 지연되어 21시경 도착 |
| 4 | 수 오전 | ✅ 게시됨 — **수동(force)**, 개선판 릴스 첫 적용분 (reelId 17870917806573144) |
| 5 | 수 저녁 | ✅ 게시됨 — **자동(schedule)** |
| 6 | 목 오전 | ✅ 게시됨 — **자동(schedule)** |
| 7 | 목 저녁 | ✅ 게시됨 — **자동(schedule)** |
| 8 | 금 오전 | ⏳ 미게시 (내일 예정) |
| 9 | 금 저녁 | ⏳ 미게시 (내일 예정) |
| 캐러셀 | 금 20시 | ⏳ 미게시 (내일 예정, `carousel.picks`도 아직 빈 배열) |

**슬롯4부터가 "완주율 최적화" 개정판 릴스** — 슬롯2~3(구버전)과 슬롯4~7(신버전)의 인사이트(평균 조회시간)를 비교하면 개선 효과를 판단할 수 있는 시점. 다음 세션 시작 시 이 인사이트 비교부터 확인하면 좋음(사용자가 인스타 앱에서 각 릴스 인사이트 스크린샷을 공유하는 방식으로 지금까지 진행됨).

**로컬 저장소 상태**: `git status` clean, `git diff` 없음, `HEAD == origin/main`(8f87f1c). 08-04~08-06 사이 커밋은 전부 파이프라인 자동 커밋(`gen:`/`state:`)뿐이며 **코드 변경은 없음** — 위 아키텍처 설명은 여전히 최신 코드와 일치.

---

## 6. 남은 문제 / 다음 작업

1. **완주율 개선 검증 대기** — 슬롯4~7(신버전) 인사이트가 아직 충분히 쌓이지 않음. 5~10편 누적 후 평균 조회시간·건너뛰기 비율 추세를 다시 확인해야 함(1편 비교는 표본 부족, 사용자가 공유한 가이드도 "반복되는 패턴이 진짜 원인"이라고 강조).
2. **참여율(좋아요/저장/공유) 0** — 미해결. 정적 릴스 구조의 한계일 가능성. 저장 유도 문구 등 추가 실험 여지 있음.
3. **크론 정시성** — 현재는 지연 감수(1~2시간) + force 버튼으로 대응 중. 외부 스케줄러(cron-job.org 등으로 workflow_dispatch를 정시 호출) 방안을 제시했으나 **미구현**. 사용자가 "일단 지켜보고 결정" 상태. 필요해지면: PAT(Actions 쓰기, 이 저장소 한정) 발급 → 외부 서비스에만 등록(채팅·코드에 노출 금지) → 08:00/19:00 정시에 API 호출.
4. **캐러셀 5개 선정 로직** — 현재 `pickCarousel`은 상품 점수(로켓배송·가격대) 규칙 기반. 원래 계획은 "릴스 실제 조회수 인사이트로 반응 좋은 5개를 고르는" 것이었으나, 인스타 API에서 릴스 조회수를 가져오는 부분은 미구현(권한·구현 복잡도 이슈로 보류). 지금은 간단 규칙으로 대체 중 — 데이터 쌓이면 고도화 검토.
5. **금요일 캐러셀 아직 미검증** — 이번 주 첫 캐러셀이 아직 게시 전. 게시되면 카드 렌더·링크 정확성·waitReady 등 실제 라이브 확인 필요.
6. **모션덱 스타일 릴스 POC — 제작 완료, 사용자 확인/채택 결정 대기 (2026-08-07).**
   "정지 카드+켄번스 줌" 대신 "애니메이션 HTML 타임라인 → Puppeteer 프레임 캡처 → ffmpeg mp4"로 만드는 방식을 상품 1개(`week-pool.json` products[0], 모니터 받침대)로 실제 구현해 현재 버전과 나란히 비교 가능한 상태.

   **추가된 파일 (라이브 파이프라인은 전혀 건드리지 않음 — `reel.js`/`cards/*` 무수정)**
   - `src/motion/deck.js` — 씬 전체가 든 애니메이션 HTML 생성기
   - `src/motion/renderMotionReel.js` — TTS→씬길이 산정→프레임 캡처→인코딩→오디오 믹스
   - `scripts/poc-motion-reel.js` — 비교 실행기 (`--stills` 디자인만 빠르게 / `--motion` / `--legacy` / `--i N` 상품 선택)
   - `.gitignore` 에 `published/poc/` 추가 (POC mp4가 리포에 들어가지 않게)

   **핵심 기술 결정**
   - 모든 모션을 Web Animations API 로 등록 후 즉시 `pause()`, `window.__seek(ms)` 가 모든 애니메이션의 `currentTime` 을 같은 값으로 밀어넣는다 → **프레임이 완전히 결정론적**(렌더 속도·실시간 요소와 무관).
   - 등장은 `fill:'both'`, 퇴장은 `fill:'forwards'` (퇴장이 등장을 덮어쓰는 문제 회피).
   - transform 을 동시에 건드리면 나중 것이 이기므로 **등장/켄번스/루프(둥실)는 반드시 서로 다른 중첩 엘리먼트**에 건다.
   - 프레임 병렬 캡처는 **브라우저 인스턴스를 N개** 띄워야 한다. 같은 브라우저의 탭 여러 개는 헤드리스 크롬이 활성 탭만 합성해서 백그라운드 탭의 `Page.captureScreenshot` 이 타임아웃난다(실제로 겪음).
   - 상품 이미지는 data URI 로 인라인 — 600프레임 동안 네트워크 재요청 방지.
   - 씬 길이 규칙(TTS 길이 + PAD 0.18, 하한 20초는 마지막 CTA 씬에 몰아넣기)은 기존 `reel.js` 와 **동일하게 맞춰서** 비교 조건을 같게 뒀다.

   **디자인**: 소프트 크림 유지 + 씬마다 색이 다른 blur blob 배경, 글자 단위 훅 등장, 어절 단위 마스크 리빌 자막, 이모지 스티커(원형 흰 배지)·자체 SVG 마스코트(눈 깜빡임+손 흔들기), 씬 전환은 blur+scale 크로스디졸브, 상단 진행바, 마지막 장점 씬에서 가격 카운트업.
   **릴스 원칙 반영**: 첫 프레임(0.00초)에 이미 훅·제품이 읽히고 0.4초에 등장 완료(빌드업 금지 원칙 유지). 자막은 내레이션에 뒤처지지 않게 delay 0.40 / stagger 0.042.

   **렌더 시간 실측(로컬 48코어)**: 20.4초 릴스 611프레임 기준 — 모션덱 **61초**(캡처 51 + 인코딩 7 + TTS 1.5) vs 현재 버전 **20초** → **약 3배**. 병렬 캡처 전에는 240초(12배)였음.
   ⚠️ **CI 주의**: GitHub Actions ubuntu 러너는 2~4 vCPU라 병렬도가 낮아 **릴스 1개당 4~8분** 예상(한 실행에 최대 2슬롯 → 최대 16분). 6시간 한도 대비 여유는 있으나 실측 필요.
   ⚠️ **CI 폰트/이모지 주의**: 한글 폰트와 컬러 이모지(Noto Color Emoji)가 러너에 없으면 두부(□)로 렌더된다. 채택 시 워크플로에 폰트 설치 스텝 추가 필요.

   **결과물**: `published/poc/p0/motion.mp4`, `legacy.mp4`, `compare.mp4`(좌=현재 / 우=모션덱), `deck.html`(브라우저로 열면 실시간 확인), `stills/`.

   **2차 수정 (2026-08-07, 사용자 피드백 4건 반영)** — 실제 게시된 릴스 스크린샷을 보고 나온 지적:
   1. *"주요 이미지와 텍스트가 너무 위에 있다"* → 인스타 앱 UI(위 계정명·오디오 / 아래 캡션·버튼)가 콘텐츠를 덮고 있었음. **안전 구역 도입: 위 236px / 아래 376px / 좌우 78px 를 비운다.** 모션덱은 `--safe-top/--safe-bottom` CSS 변수, 라이브 `reelTemplates.js` 는 `SAFE_TOP/SAFE_BOTTOM/SIDE` 상수. 진행바도 안전 구역 안으로 내림.
   2. *"문장이 끝나고 다음 문장까지 텀이 있다"* → 원인은 PAD 값이 아니라 **Edge TTS 가 음성 앞뒤에 무음을 문장당 0.8~1.0초씩 붙여 내보내는 것**(실측: "목이 편해짐" 1.78s 중 0.96s가 무음). ffmpeg `silenceremove`(+`areverse` 2패스)로 잘라낸 뒤 `PAD_AFTER 0.18→0.06`, `LEAD 0.06→0`, `MIN_CARD 1.2→1.0`. **모션덱과 라이브 `reel.js` 양쪽에 적용.**
      부작용 대응: 무음이 빠지니 본문이 짧아져 20초 하한을 채우려면 CTA 화면이 7.8초가 됐다(=이탈 구간). → `COVER_MIN 1.6`(훅 읽을 시간 확보) + `CTA_MAX 4.2`(상한) 도입. **20초를 못 채우면 영상이 짧아지는 쪽을 택한다**(릴스는 짧을수록 완주율 유리). 현재 6씬 17.0초.
   3. *"이미지가 사라졌다 다시 나오면 스킵할 것 같다"* → 씬 전환을 **blur 크로스페이드 → 좌우 슬라이드(0.26초)** 로 교체. 두 씬이 같은 속도로 움직여 필름 스트립처럼 이어지고 제품 사진이 화면에서 사라지지 않는다. 장점 씬의 이미지 등장 애니메이션(`data-anim="img"`)도 제거 — 슬라이드로 들어오면 그걸로 충분.
   4. *"'사라 / 사야지 좋다' 훅이 더 있으면 좋겠다"* → `buyHook` 필드 신설(6~12자 구매 유도 한마디). `caption.js` POOL_SYSTEM 프롬프트에 생성 규칙 추가 → `pipeline.js` 가 `week-pool.json` 에 저장 → 모션덱·라이브 CTA 카드 모두에서 큰 글씨로 사용. 누락 시 `BUY_FALLBACK`("이 가격이면 사야죠" 등)으로 대체하므로 이번 주 풀에도 바로 적용됨. 가격 씬에 "🛒 지금 담아두기" 배지, CTA 문구 "전체 보기"→"바로 구매", 마지막 내레이션도 "…프로필 링크에 있어요"→"마음에 들면 프로필 링크에서 바로 구매하세요" 로 변경.
      ⚠️ 허위 긴급성("품절 임박","오늘만 이 가격")은 프롬프트에서 금지 — 재고·기간을 확인할 수 없으므로 의견 표현만 허용.

   **⚠️ 이 2차 수정으로 라이브 파이프라인도 함께 바뀌었다** (1차 POC 때와 달리 `reel.js`/`reelTemplates.js`/`build.js`/`caption.js`/`pipeline.js` 수정됨). 안전 구역·텀 제거·구매 훅은 모션덱 채택 여부와 무관하게 현재 릴스에도 필요한 개선이라 함께 적용한 것. 다음 게시분부터 반영된다.

   **렌더 시간 재실측**: 모션덱 58초 / 현재 버전 21초 → **2.8배**.

   ### ✅ 채택 확정 — 파이프라인 연결 완료 (2026-08-07)
   사용자가 두 영상을 비교한 뒤 **모션덱 채택**을 결정. `pipeline.js` 의 릴스 렌더가
   `buildSingleReelCards` + `buildReel` → **`buildMotionReel`** 로 교체됨(모션덱은 카드 JPG를 거치지 않는다).

   - **롤백 스위치**: 환경변수 `REEL_STYLE=legacy` 면 구버전(정지 카드+켄번스) 경로를 그대로 탄다.
     워크플로 `workflow_dispatch` 에 `reel_style` 드롭다운(motion/legacy)을 추가해 GitHub 화면에서 즉시 되돌릴 수 있음.
     **구버전 코드(`buildSingleReelCards`/`buildReel`/`reelTemplates.js`)는 지우지 말 것** — 롤백 경로다.
   - **`deck.html` 은 임시폴더에만 쓴다**(기본). 상품 이미지를 data URI 로 품고 있어 3~6MB인데,
     워크플로가 `published/` 를 통째로 커밋하므로 그대로 두면 public 저장소가 주당 30MB씩 불어난다.
     브라우저로 애니메이션을 직접 볼 때만 `keepHtml: true` (POC 스크립트가 사용).
   - **표지 첫 프레임 강화**: 첫 프레임이 곧 릴스 썸네일이자 스크롤 스토퍼라
     `riseFast`/`imgFast` 시작 opacity 를 0.2/0.3 → 0.55/0.6 으로 올려 0프레임에도 훅·제품이 또렷이 읽히게 함.
   - **미검증(다음 세션에서 반드시 확인)**: CI(ubuntu 러너, 4 vCPU)에서의 실제 렌더 시간·성공 여부.
     로컬 48코어 기준 릴스 1개 52초이나 러너는 병렬도가 낮아 3~6분 예상. 한 실행 최대 2슬롯.
     **오늘(금) 08시 실행이 모션덱 첫 클라우드 렌더** — 실패하면 `reel_style=legacy` 로 수동 재실행.
     컬러 이모지 폰트(`fonts-noto-color-emoji`)도 이번에 처음 적용되므로 이모지 스티커가 컬러로 나오는지 같이 확인.
6-1. **반려동물 전환 (2026-08-12 결정·구현)** — 참여 0 + 팔로워(구매 팔로워, 지속 감소)를 근거로 사용자가 카테고리 전면 교체 결정. `categories.js` 4종(dog-play/cat-play/dog-life/cat-life), `caption.js` 예시 문구 펫 톤, `deck.js` 리디자인(따뜻한 파스텔 팔레트 + 강아지/고양이 SVG 마스코트 — 카테고리명으로 자동 선택, 꼬리 살랑·귀 씰룩·발자국 트레일·펫 이모지). **비-펫 카테고리는 중립 마스코트·기존 이모지로 자동 폴백**하므로 이번 주 잔여 슬롯 안전. 표지 등장은 실전 표지 길이(1.6초)에 맞춰 1.1초 내 완료로 조정. 새 풀은 8/17(월)부터. 미리보기: `node scripts/preview-pet-deck.js` (실제 쿠팡 펫 상품으로 강아지/고양이 스틸 생성). 참고: 8/10~11 "발행 안 됨"의 원인은 콘텐츠가 아니라 **메타 개발자 계정 잠금("API access blocked")**이었음 — 계정 확인 + 토큰 재발급으로 8/12 해결, 잠금 중 밀린 슬롯은 force 실행으로 소진 시작.

7. **레거시 정리 여지** — `postedLog.js`의 `postedTodayKST` 등 구버전 가드가 새 슬롯 기반 가드로 대체된 후에도 코드에 남아있을 수 있음(사용 여부 미확인). `selectProducts.js`의 구버전 `pickCategory`/`selectProducts`도 `selectWeeklyPool` 도입 후 미사용 상태로 추정 — 필요시 정리.
8. **연장 아이디어(합의만 됨, 미착수)**: DM 자동화(댓글→DM), IG 토큰 60일 자동 갱신, 유튜브 쇼츠 크로스포스트.

---

## 7. 보안/운영 규칙 (반드시 지킬 것)

- API 키·토큰은 로컬 `.env`(gitignore) 또는 GitHub Secrets에만. 채팅·코드에 노출 금지 (과거 사용자가 PAT·쿠팡 키를 실수로 채팅에 붙여넣은 적 있음 — 그때마다 즉시 폐기/재발급 안내함).
- 대가성 문구(`DISCLOSURE` 상수, 공정위 규정 문구) 없는 게시물은 `validateCaption`이 차단.
- 허위 후기 표현("직접 써봤다"/"사용해보니"/"내돈내산"/"후기") 및 효능·건강 단정 문구는 프롬프트 레벨에서 금지, 감지 시 생성 실패 처리.
- 사용자는 비개발자 — GUI 단계별 안내, 캡처 스크린샷 대조, "F5/새로고침" 같은 용어도 쉽게 풀어서 설명할 것.
- git 커밋 시 CI가 되돌린 `data/`, `docs/`, `published/` 변경분과 로컬 작업이 겹칠 수 있으므로, **커밋 전 항상 `git pull --rebase origin main`으로 클라우드 상태와 동기화**할 것(특히 `week-pool.json`처럼 게시 상태를 담은 파일을 실수로 되돌리지 않도록 `git status`로 확인 후 필요하면 `git restore`).
